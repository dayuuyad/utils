pipeline {
    agent any

    stages {

            stage('SonarQube Analysis') {
                steps {
                    script {
                         scannerHome = tool 'sonarqubeScanner'
                    }
                    withSonarQubeEnv('sonarqube') {
//                        powershell '${scannerHome}\\sonar-scanner.bat'
                        powershell "\"${scannerHome}\\bin\\sonar-scanner.bat\""

                    }
                }
            }
    }

}